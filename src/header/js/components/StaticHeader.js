import React from 'react';
import Logo from './Logo';

const StaticHeader = () => (
  <header className="siteheader-checkout">
    <div className="header-checkout-container header-checkout-container--responsive_bottom">
      <Logo />
    </div>
  </header>
);

export default StaticHeader;
